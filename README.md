# img_org
A simple CLI image organizer. It searches a source directory for any and all images it can find. It then copies them to a destination directory and sorts them via their exifdata by year and month taken.


## How to use
Install:
```
git clone
pip install -r requirements.txt
```

Run:
```
python3 img_org.py
```
follow prompts on screen